#ifndef SIMILARITY_H
#define SIMILARITY_H
#include <stdio.h>
#include <stdlib.h>

#include "wave.h"


int EuclideanDistance(WAV* f1,WAV* f2);
int LCSS( WAV *f1,WAV *f2);
int max(int a, int b);
int min(int a, int b);
#endif
